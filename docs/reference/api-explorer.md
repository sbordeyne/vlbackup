# API Explorer

Interactive view of the VLBackup API, rendered directly from
[`openapi/schema.yaml`](https://github.com/sbordeyne/vlbackup/blob/master/openapi/schema.yaml)
— the schema is the single source of truth, so this page never drifts from the
generated server. For prose and `curl` examples, see the [HTTP API](http-api.md).

[OAD(./openapi/schema.yaml)]
